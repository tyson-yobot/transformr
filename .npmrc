# Monorepo npm workspace configuration.
# legacy-peer-deps avoids peer dependency conflicts between workspace packages.
# install-strategy=hoisted (default) allows transitive deps to be visible
# to Expo autolinking, which only scans top-level node_modules.
legacy-peer-deps=true
